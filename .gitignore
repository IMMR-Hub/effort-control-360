# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage
/test-results
/playwright-report
/blob-report

# Production
/dist
/build

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db

# Build output de TypeScript
dist/
*.tsbuildinfo

# Entorno
.env
.env.*
!.env.example

# Datos reales de clientes (muestras para definir formatos de importación).
# Nunca al historial de git: RUCs, balances y declaraciones reales.
docs/Muestras/

# Respaldos de la base: contienen datos reales de EFFORT (nombres, RUC, importes).
# Nunca van a git — el repositorio no es el lugar para datos de clientes.
respaldos/

# Scripts temporales de diagnóstico dentro de apps/api. Los de verdad viven en
# scripts/ y pasan el lint como cualquier otro archivo del proyecto.
apps/api/*.mjs
